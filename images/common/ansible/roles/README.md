# Local roles

- `common_baseline`: lightweight baseline steps + manifest writing
- `common_agents_stub`: placeholder for adding org-specific agents later

CIS roles are pulled via `requirements.yml` and referenced by name.
