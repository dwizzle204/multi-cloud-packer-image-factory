# Inventory samples

These files are optional helpers for running Ansible outside of Packer.

- `linux.ini.example`: sample SSH inventory for Linux image validation.
- `windows.ini.example`: sample WinRM inventory for Windows validation.

Do not commit live credentials or production hostnames.
